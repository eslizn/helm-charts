apiVersion: v1
kind: Service
metadata:
  name: {{ include "aktools.fullname" . }}
  labels:
    {{- include "aktools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aktools.selectorLabels" . | nindent 4 }}
